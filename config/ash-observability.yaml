version: "ash.obs/v0.1"

redaction:
  enabled: true
  denyKeys:
    - password
    - secret
    - token
    - authorization
    - apiKey
    - api_key
  maskValue: "[REDACTED]"

sampling:
  eventExportRate: 1

export:
  allowOutbound: false

plugins:
  prometheus:
    enabled: true
    path: /metrics
  console:
    enabled: true
    level: info
  otel:
    enabled: false
    # endpoint: localhost:4317
    # insecure: true
    # Or set ASH_OTEL_ENABLED=1 and ASH_OTEL_ENDPOINT=localhost:4317
