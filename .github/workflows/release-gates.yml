name: Live Worker Gate

on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  worker-local-gate:
    name: Ephemeral Worker live smoke
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true

      - name: Worker local gate
        run: make worker-local-gate

  mvp-signoff:
    name: MVP signoff (static gates)
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: MVP signoff static
        env:
          ASH_RELEASE_AUDIT_SKIP_OPENAPI: "1"
        run: make mvp-signoff
