# v2.5 RAG + Sandbox (DX16–DX18) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Deliver ctags symbol indexing (DX16), Qdrant vector lane POC (DX17), and Linux Landlock sandbox executor (DX18) on the v2.4 baseline; DX15 is already complete.

**Architecture:** Extend `internal/rag` with pluggable symbol + vector lanes merged via existing `rrfMerge`; extend `internal/sandbox` router with optional Landlock executor behind env + capability probe. SQL rev 30–31 with RLS sync.

**Tech Stack:** Go 1.26, GORM, Postgres/SQLite migrations, React console (DX17 profile fields only), Bash smoke gates.

**Spec:** [`docs/superpowers/specs/2026-09-03-v25-dx15-dx18-design.md`](../specs/2026-09-03-v25-dx15-dx18-design.md) · Scope [`doc/plan/v2.5-release-scope.md`](../../../doc/plan/v2.5-release-scope.md)

## Global Constraints

- Commit messages in Chinese; do not push unless asked
- Shell: Git Bash; `export GOPROXY=https://goproxy.cn,direct` if proxy fails
- New Postgres tables: SQL rev bump + RLS in `000013` pattern + Doctor M3-11 if applicable
- `PostgresRLSDeferredTables()` must stay empty
- Public API: handler swagger comments → `make swagger` → `doc/api/openapi-ash-v1.yaml` → `make openapi-check`
- Degrade paths: ctags/Qdrant/Landlock unavailable → no 5xx; omit lane/executor
- Tag `v2.5.0` is **manual** (DX19 signoff sprint)

## Sequencing

```
DX15 ✅ → DX16 (SQL 30) → DX17 (SQL 31 / RLS 52) → DX18 (no schema) → DX19 (freeze)
```

---

## DX16 — ctags symbol indexer

### Task 1: Schema `rag_symbols.source`

**Files:**
- Create: `internal/store/sqlmigrations/migrations/postgres/000030_rag_symbols_source.up.sql`
- Create: `internal/store/sqlmigrations/migrations/postgres/000030_rag_symbols_source.down.sql`
- Modify: `internal/store/sqlmigrations/migrate.go` (`expectedVersion` → **30**)
- Modify: `internal/store/models.go` (`RAGSymbol.Source string`)
- Modify: `internal/store/rls_migration_test.go` if policy count unchanged (50)

**Interfaces:**
- Produces: `store.RAGSymbol` with `Source` column (`regex` | `ctags`)

- [ ] **Step 1:** Add migration `ALTER TABLE rag_symbols ADD COLUMN IF NOT EXISTS source TEXT NOT NULL DEFAULT 'regex';`
- [ ] **Step 2:** Bump `expectedVersion` to 30; GORM model `Source string gorm:"not null;default:regex"`
- [ ] **Step 3:** `go test ./internal/store/... -count=1`

### Task 2: SymbolIndexer + CtagsIndexer

**Files:**
- Create: `internal/rag/symbols_indexer.go`
- Create: `internal/rag/symbols_indexer_test.go`
- Create: `scripts/fixtures/fake-ctags.sh` (prints ctags `-x` lines for tests)
- Modify: `internal/rag/rebuild.go`
- Modify: `internal/rag/rebuild_test.go`

**Interfaces:**
- Produces:
  ```go
  type SymbolHit struct { Name, Kind, Line int }
  type SymbolIndexer interface { IndexFile(path string, content []byte) ([]SymbolHit, error); Name() string }
  func ResolveSymbolIndexer() SymbolIndexer // regex default; ctags when ASH_RAG_CTAGS=1 or on PATH
  ```

- [ ] **Step 1:** Write failing test `TestCtagsIndexerParsesFixture` using fake script as `CTAGS` env
- [ ] **Step 2:** Implement `RegexIndexer` (wrap `matchSymbolLine`) and `CtagsIndexer` subprocess
- [ ] **Step 3:** `ResolveSymbolIndexer`: `ASH_RAG_CTAGS=0` forces regex; `=1` or auto-detect `ctags`/`universal-ctags`
- [ ] **Step 4:** Refactor `RebuildSymbols` loop to call indexer; set `sym.Source = indexer.Name()`
- [ ] **Step 5:** Extend `RebuildSymbolsResponse` with `SymbolSource string json:"symbolSource"`
- [ ] **Step 6:** `go test ./internal/rag/... -count=1`

### Task 3: API + smoke + docs

**Files:**
- Modify: `internal/api/rag.go` (if response type duplicated)
- Modify: `doc/api/openapi-ash-v1.yaml` (`symbolSource` on rebuild response)
- Modify: `scripts/rag-hybrid-smoke.sh`
- Create: `doc/plan/sprint-dx16-rag-ctags.md`

- [ ] **Step 1:** `make swagger && make openapi-check`
- [ ] **Step 2:** Extend `rag-hybrid-smoke` to assert `symbolSource` field present
- [ ] **Step 3:** Sprint doc + CHANGELOG DX16 entry

---

## DX17 — Vector retrieval POC

### Task 4: Schema `rag_vector_refs`

**Files:**
- Create: `internal/store/sqlmigrations/migrations/postgres/000031_rag_vector_refs.up.sql`
- Create: `internal/store/sqlmigrations/migrations/postgres/000031_rag_vector_refs.down.sql`
- Modify: `internal/store/sqlmigrations/migrate.go` (`expectedVersion` → **31**)
- Modify: `internal/store/models.go` (`RAGVectorRef`)
- Modify: `internal/store/rls.go` (`PostgresRLSTables` +2 policies → **52**)
- Modify: `internal/store/rls_migration_test.go` (expect 52)

### Task 5: Embedder + Qdrant client

**Files:**
- Create: `internal/rag/embedder.go` + `_test.go` (`HashEmbedder` deterministic dims e.g. 64)
- Create: `internal/rag/qdrant.go` + `_test.go` (HTTP upsert/search; mock transport in tests)

**Interfaces:**
- Produces:
  ```go
  type Embedder interface { Embed(texts []string) ([][]float32, error); Dim() int }
  type VectorStore interface { Upsert(space, collection string, points []VectorPoint) error; Search(space, collection string, vec []float32, topK int) ([]VectorHit, error); Available() bool }
  ```

### Task 6: Index + Query vector lane

**Files:**
- Modify: `internal/rag/service.go` (`Query` adds 4th lane `"vector"` when store available)
- Modify: `internal/rag/hybrid.go` (constant `RetrievalModeHybridVector = "hybrid+vector"`)
- Modify: `internal/rag/profile.go` (`vectorAvailable`, `vectorPointCount`)
- Modify: `internal/api/rag.go` (index body `embed` bool)
- Create: `scripts/rag-vector-smoke.sh`
- Modify: `Makefile` (`rag-vector-smoke` target)

- [ ] **Step 1:** Failing test `TestQueryVectorLaneWhenQdrantMocked`
- [ ] **Step 2:** Implement index path: chunk → embed → qdrant upsert + `rag_vector_refs` row
- [ ] **Step 3:** Query path: embed query text → search → map to `Hit` with lane `vector`
- [ ] **Step 4:** Qdrant down → skip lane (same as empty hybrid)
- [ ] **Step 5:** `make rag-vector-smoke` green

---

## DX18 — Landlock sandbox POC

### Task 7: Landlock executor (Linux)

**Files:**
- Create: `internal/sandbox/landlock/available_linux.go` (`//go:build linux`)
- Create: `internal/sandbox/landlock/available_other.go` (`//go:build !linux`)
- Create: `internal/sandbox/landlock/executor_linux.go`
- Create: `internal/sandbox/landlock/executor_test.go` (linux-only or build-tagged)
- Modify: `internal/sandbox/router.go` (prefer `landlock` when `ASH_SANDBOX_LANDLOCK=1` && isolated && Available)
- Modify: `internal/toolbus/` dispatch wiring if executor switch lives there

- [ ] **Step 1:** `landlock.Available()` — kernel ≥ 5.13 probe; false on non-Linux
- [ ] **Step 2:** `landlock.Executor` implements `sandbox.Executor` with FS rules under `RepoRoot`
- [ ] **Step 3:** Router returns `Executor: "landlock"` before docker/process when enabled
- [ ] **Step 4:** Doctor **M4-SBX-04** case (skip on Windows/macOS; pass/warn on Linux)

### Task 8: Smoke + docs

**Files:**
- Modify: `scripts/sandbox-smoke.sh` (optional `ASH_SANDBOX_LANDLOCK=1` segment)
- Create: `doc/plan/sprint-dx18-landlock.md`
- Modify: `doc/checklists/sandbox-smoke.md`

- [ ] **Step 1:** `make sandbox-smoke` green (skip Landlock segment on non-Linux)
- [ ] **Step 2:** Sprint doc + CHANGELOG DX18 entry

---

## DX19 — v2.5 freeze (after DX16–18)

**Files:**
- Modify: `doc/plan/v2.5-release-scope.md` → **已冻结**
- Create: `scripts/v25-signoff-gate.sh`, `make v2.5-signoff`
- Create: `doc/checklists/v2.5-signoff.md`, `doc/evidence/v2.5-signatures-template.md`
- Modify: `scripts/scope-freeze-gate.sh` (v2.5)
- Modify: `doc/plan/PLAN-进度与里程碑.md`, `TODO.md`

Gate:

```bash
make scope-freeze-gate
make v2.5-signoff   # openapi + doctor ALL + waker + rag-hybrid + rag-vector + sandbox
```

---

## Verification matrix (per sprint)

| Sprint | Command |
|--------|---------|
| DX16 | `go test ./internal/rag/... -count=1` · `make rag-hybrid-smoke` |
| DX17 | `go test ./internal/rag/... -count=1` · `make rag-vector-smoke` |
| DX18 | `go test ./internal/sandbox/... -count=1` · `make sandbox-smoke` |
| DX19 | `make v2.5-signoff` |
