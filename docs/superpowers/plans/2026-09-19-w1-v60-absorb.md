# W1 v6.0 Absorb Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans. Checkbox steps.

**Goal:** 交付 v6.0 吸收 P0：声明式 `ash.hooks.v1`（PreToolUse）、ExecPolicy 骨架并入沙箱地板、Steer/Queue 会话意图与 Chat 投影。

**Architecture:** Hooks/ExecPolicy 配置落在 SpacePolicy `BodyJSON`（可后续迁独立表）；Hooks 在 `runs/execute` 工具调用前求值；Steer/Queue 扩展 `session.Intent`；UI 只发意图、只渲事件。

**Tech Stack:** Go · Gin · 现有 runs/session/spacepolicy/sandbox · React AgentChatShell

**Board:** [`doc/plan/sprint-ew-w1-v60-absorb.md`](../../doc/plan/sprint-ew-w1-v60-absorb.md)

## Global Constraints

- fail-closed；禁止 YOLO 默认
- 不引入 Cordis；不做 fork/compact UX（W2）
- W0 审批预设 / MCP gate 行为不得回退
- 中文 commit；不提交 smoke-latest、archify 无关产物
- V6-D2/D3：Hooks 可审计；ExecPolicy 与现有策略合并取更严

---

### Task EW11: Hooks 协议骨架

**Files:**
- Create: `internal/hooks/types.go`, `internal/hooks/evaluate.go`, `internal/hooks/load.go`, `*_test.go`
- Modify: `internal/spacepolicy/service.go`（可选 helper 读 hooks 段）
- Modify: `CHANGELOG.md`, `doc/plan/sprint-ew-w1-v60-absorb.md` 勾选

**Interfaces:**
```go
package hooks

const SchemaVersion = "ash.hooks.v1"

type Event string
const (
  EventPreToolUse Event = "PreToolUse"
)

type DecisionAction string
const (
  ActionAllow DecisionAction = "allow"
  ActionDeny  DecisionAction = "deny"
  ActionAsk   DecisionAction = "ask"
)

type Rule struct {
  Event   Event  `json:"event"`
  Tool    string `json:"tool,omitempty"`    // name or glob; empty = any
  Risk    string `json:"risk,omitempty"`    // low|medium|high|critical
  Action  DecisionAction `json:"action"`
  Reason  string `json:"reason,omitempty"`
}

type Config struct {
  Version string `json:"version"` // ash.hooks.v1
  Rules   []Rule `json:"rules"`
}

type ToolContext struct {
  Tool string
  Risk string
  SpaceID string
  RunID string
  StepID string
}

type Decision struct {
  Action DecisionAction
  Reason string
  RuleIndex int // -1 if default allow (no match)
}

func ParseConfigJSON(body string) (Config, error)
func ConfigFromSpaceBodyJSON(bodyJSON string) (Config, error) // reads key "hooks"
func Evaluate(cfg Config, ev Event, ctx ToolContext) Decision
```

行为：
- 无 `hooks` 键或空 rules → Evaluate 对 PreToolUse 返回 `allow`（不改变现状）
- 多规则：按顺序首个匹配（tool glob + 可选 risk）生效
- `version` 非空且非 `ash.hooks.v1` → Parse 报错（fail-closed 加载）

- [ ] **Step 1:** 写失败测试 `TestEvaluate_DenyBash` / `TestEvaluate_NoConfigAllow` / `TestParse_BadVersion`
- [ ] **Step 2:** 实现 types + Parse + Evaluate（tool 匹配：精确或 `*` 后缀前缀简单 glob）
- [ ] **Step 3:** `go test ./internal/hooks/... -count=1`
- [ ] **Step 4:** Commit `feat(hooks): ash.hooks.v1 声明式骨架与 PreToolUse 求值`
- [ ] **Step 5:** 勾选板 EW11-*

---

### Task EW12: PreToolUse 接入 execute

**Files:** `internal/runs/execute.go`, `internal/runs/*_test.go`, wire SpacePolicy hooks load

- [x] 在工具调用前加载空间 hooks → Evaluate PreToolUse
- [x] deny：追加事件并跳过/失败该步（与危险工具拒绝一致风格）
- [x] ask：进入现有 approval 路径
- [x] 测试 + Commit `feat(runs): PreToolUse hook 接入工具链`

---

### Task EW13: 审计事件 + 薄投影

**Files:** events 类型稳定；FE Trajectory/Details 识别 `hook.*`；文档

- [ ] 事件字段文档化
- [ ] 最小 FE 展示
- [ ] Commit `feat: Hooks 决策事件可观测`

---

### Task EW14: ExecPolicy schema

**Files:** `internal/execpolicy/` Parse + Merge；SpacePolicy BodyJSON `execPolicy`

- [ ] schema + 更严合并单测
- [ ] Commit `feat(execpolicy): v1 声明与策略合并`

---

### Task EW15: ExecPolicy → sandbox + Doctor

**Files:** `internal/sandbox/policy.go`, doctor/scale readiness

- [ ] 地板提升；无配置不降级
- [ ] Commit `feat: ExecPolicy 驱动沙箱地板与 Doctor 位`

---

### Task EW16: Steer intent

**Files:** `internal/session/intent.go`, service PromptTurn cancel, events, FE composer

- [ ] `action=steer` + `session.steer` 事件
- [ ] Commit `feat(session): steer 运行中续写意图`

---

### Task EW17: Queue intent + Chat

**Files:** session meta queue, IntentBar/Composer chip

- [ ] `action=queue`；完成后自动消费
- [ ] Commit `feat(session): follow-up 队列意图与 Chat 投影`

---

### Task EW18: W1 signoff

- [ ] openapi-check · 全相关测 · CHANGELOG · TODO · inventory
- [ ] Commit `docs: W1 v6.0 吸收签字`

---

## Self-Review

| Inventory W1 | Tasks |
|--------------|-------|
| Hooks | EW11–13 |
| ExecPolicy | EW14–15 |
| Steer/Queue | EW16–17 |
| Signoff | EW18 |

## Execution

SDD：一次一个 Task；从 **EW11** 开始。
