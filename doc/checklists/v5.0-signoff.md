# ASH v5.0 / v5.2–5.3 signoff checklist

> 程序：[`../plan/v5-governance-program.md`](../plan/v5-governance-program.md)  
> 计划：[`../../docs/superpowers/plans/2026-09-13-v5-governance-implementation.md`](../../docs/superpowers/plans/2026-09-13-v5-governance-implementation.md)

## 自动门禁（`make v5-signoff`）

- [x] `go test ./internal/registry/... ./internal/spacepolicy/... ./internal/scoring/... ./internal/evolve/... ./internal/orgtemplates/... -count=1`
- [x] `go test ./internal/doctor/... -run TestTR3Suite -count=1`
- [x] `make swagger && make openapi-check`
- [x] `cd frontend && npx vitest --run` Reviews / Space / Scale / ImproveProposalsPane / MobileReviews
- [x] **FE-45**：`npx tsc -b && npx vite build`（门禁内；Makefile `web-build` 同步改为直接 npx，避开 Win/make 下 `npm run build` 误失败）

## 功能收口（实现已落地）

- [x] **FE-44**：Reviews / Mobile / Observability / Registry / Quest 中文空态与权限提示（`658771d`）
- [x] **score_appeal（BE-18 remainder）**：audit-backed `appeal` 队列 · keep/void · 无 SQL 迁移（`13658f4`）
- [x] **FE-45**：全量 `web-build` 回归（2026-09-14）

## 自动/单测证据（原手工项，已由自动化覆盖）

- [x] Space 页可见 Registry 资产列表与 EffectivePolicy 摘要  
  - FE：`SpacePage` → `renders Registry assets panel and EffectivePolicy summary`（`registry-assets-panel` / `effective-policy-summary`）  
  - Doctor：TR3-12 registry list；TR3-13 `effectivePolicy` / `policyMerge`
- [x] Reviews Workbench 三栏；批准携带四维 rubric  
  - FE：`ReviewsPage` → `renders workbench columns and submits rubric`  
  - Mobile（FE-16）：`MobileReviewsPage` → `shows compact rubric and assign on expanded card`
- [x] team + multiSign：首次批准 → `pending_second`；第二人批准才生效  
  - BE：`TestMultiSignPendingSecond`（`internal/evolve/service_test.go`）  
  - FE：`ReviewsPage` → `shows pending_second badge and second-sign decide labels`；`MobileReviewsPage` → pending_second 徽章
- [x] 低分 reject/低 composite → Improve draft（`source=low_score`）  
  - BE：`TestLowScoreCreatesImproveDraft`（`internal/evolve/service_test.go`）  
  - FE：`ImproveProposalsPane` → `shows muted low_score source line for auto drafts` + helpers
- [x] promote / memory approve 仍为人审闸门（无自动升格）  
  - Doctor：`M5-EVO-03` / `promoteGate`（`m5Evo03PromoteNeedsReview`：未进审不可 Promote）  
  - Memory：`TestCandidateReviewFlow`（候选需显式 `Review(approve)` 后才可查询命中）  
  - 不新增产品行为；仅引用既有证据

## 建议人工扫一眼（UI-only，可选）

- [ ] 建议人工扫一眼：Space / Reviews / Mobile 在真实 Worker + JWT 下的视觉与权限提示（单测已覆盖主路径文案与控件）
- [ ] 建议人工扫一眼：Improve 低分草稿在控制台列表中的 muted 行（vitest 已覆盖 `improve-low-score-source`）

## 证据

- Doctor TR3-12：`spaceKind` + registry list（可空）
- Doctor TR3-13：`effectivePolicy` / `policyMerge` / `rubricSchema` / `scoreEvent`
- SQL revisions：36 registry · 37 policy packs · 38 score_events
- **2026-09-14**：`make v5-signoff` 通过（Go 包 + TR3 + openapi + Reviews/Space/Scale vitest 20 tests）；门禁脚本改为 `npx vitest`（与 v3x/v40 一致，避免 Win/Git Bash 下 `npm test` 经 make 误失败）
- **2026-09-14**：FE-45 closeout — `make web-build` OK；`make v5-signoff` OK（vitest 5 files / 29 tests：Reviews/Space/Scale/Improve/Mobile + web-build）；SpacePage 增 Registry/EffectivePolicy 断言；原手工项改挂自动证据
