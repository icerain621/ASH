# v4.1 发布签字清单 / Sign-off checklist（Sprint DX72）

> 目标：冻结 [`v4.1-release-scope.md`](../plan/v4.1-release-scope.md)，跑通 `make v4.1-signoff`，**人工确认后再打 `v4.1.0` tag**。  
> Freeze the v4.1 scope, pass `make v4.1-signoff`, then tag **`v4.1.0` by hand**.

## 前置 / Preconditions

| # | 项 | 命令 / 文件 | 期望 |
|---|-----|-------------|------|
| 1 | 范围文档 | `doc/plan/v4.1-release-scope.md` | 含 §1–§6；状态「已冻结」 |
| 2 | 结构门禁 | `make scope-freeze-gate` | 含 v4.1 |
| 3 | v4.1 门禁 | `make v4.1-signoff` | 配额 / 审计报表 / spawn 策略测试绿；不自动打 tag |

## 签字 / Sign-off

1. 过一遍 scope §2 In（DX67–DX72）/ §3 Out（无真计费）。  
2. 填写 [`../evidence/v4.1-signatures-template.md`](../evidence/v4.1-signatures-template.md)。  
3. 产品确认范围冻结。

## Tag（人工）

```bash
git tag -a v4.1.0 -m "ASH v4.1.0 enterprise Agentic quotas audit-report spawn policy (DX67–DX72 frozen)"
git push origin v4.1.0
```

本清单与 `make v4.1-signoff` **不会**自动执行 `git tag`。
