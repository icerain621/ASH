# v2.1 发布签字清单（Sprint DX5）

> 目标：冻结 [`v2.1-release-scope.md`](../plan/v2.1-release-scope.md)，跑通 `make v2.1-signoff`，**人工确认后再打 `v2.1.0` tag**。

## 前置

| # | 项 | 命令 / 文件 | 期望 |
|---|-----|-------------|------|
| 1 | 范围文档 | `doc/plan/v2.1-release-scope.md` | 含 §1–§6；状态「已冻结」 |
| 2 | 结构门禁 | `make scope-freeze-gate` | MVP + v2 + v2.1 均 OK |
| 3 | v2.1 门禁 | `make v2.1-signoff` | Doctor ALL **55** / M4 **8** / acp-smoke 绿 |
| 4 | （可选） | `make v2-signoff` | v2.0 门禁仍绿 |

## 签字会议（建议 20–30min）

1. 过一遍 v2.1 scope §2 In（DW–DX5）/ §3 Out  
2. 确认 Out 项（Landlock、生产 ACP 服务、云 live）不阻塞功能 tag  
3. 填写 [`../evidence/v2.1-signatures-template.md`](../evidence/v2.1-signatures-template.md) → 复制为 `v2.1-signatures-latest.md`  
4. 产品确认范围冻结

## Tag（人工）

```bash
git status -sb
git tag -a v2.1.0 -m "ASH v2.1.0 ACP bridge (DW–DX5 frozen)"
git push origin v2.1.0
```

本清单与 `make v2.1-signoff` **不会**自动执行 `git tag`。

## 相关

- 计划：`doc/plan/sprint-dx5-v21-signoff.md`
- v2.0 对照：[`v2-signoff.md`](v2-signoff.md)
