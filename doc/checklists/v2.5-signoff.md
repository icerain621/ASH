# v2.5 发布签字清单（Sprint DX19）

> 目标：冻结 [`v2.5-release-scope.md`](../plan/v2.5-release-scope.md)，跑通 `make v2.5-signoff`，**人工确认后再打 `v2.5.0` tag**。

## 前置

| # | 项 | 命令 / 文件 | 期望 |
|---|-----|-------------|------|
| 1 | 范围文档 | `doc/plan/v2.5-release-scope.md` | 含 §1–§6；状态「已冻结」 |
| 2 | 结构门禁 | `make scope-freeze-gate` | MVP + v2 … + v2.5 均 OK |
| 3 | v2.5 门禁 | `make v2.5-signoff` | Doctor ALL **56** / M4 **9** / waker + rag-hybrid + rag-vector + sandbox 绿 |
| 4 | （可选） | `make v2.4-signoff` | v2.4 门禁仍绿 |

## 签字会议（建议 20–30min）

1. 过一遍 v2.5 scope §2 In（DX15–DX19）/ §3 Out  
2. 确认 Out 项（真实 embedding 硬依赖、E2B、云 live、LSP）不阻塞功能 tag  
3. 填写 [`../evidence/v2.5-signatures-template.md`](../evidence/v2.5-signatures-template.md) → 复制为 `v2.5-signatures-latest.md`  
4. 产品确认范围冻结

## Tag（人工）

```bash
git status -sb
git tag -a v2.5.0 -m "ASH v2.5.0 Waker probes + ctags + vector + Landlock (DX15–DX19 frozen)"
git push origin v2.5.0
```

本清单与 `make v2.5-signoff` **不会**自动执行 `git tag`。

## 相关

- 计划：`doc/plan/sprint-dx19-v25-signoff.md`
- v2.4 对照：[`v2.4-signoff.md`](v2.4-signoff.md)
