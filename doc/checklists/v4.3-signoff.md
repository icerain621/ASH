# v4.3 发布签字清单 / Sign-off checklist（Sprint DX84）

> 目标：冻结 [`v4.3-release-scope.md`](../plan/v4.3-release-scope.md)，跑通 `make v4.3-signoff`，**人工确认后再打 `v4.3.0` tag**。  
> Freeze the v4.3 scope, pass `make v4.3-signoff`, then tag **`v4.3.0` by hand**.

## 前置 / Preconditions

| # | 项 | 命令 / 文件 | 期望 |
|---|-----|-------------|------|
| 1 | 范围文档 | `doc/plan/v4.3-release-scope.md` | 含 §1–§6；状态「已冻结」 |
| 2 | 结构门禁 | `make scope-freeze-gate` | 含 v4.3 |
| 3 | v4.3 门禁 | `make v4.3-signoff` | catalog 私有标记 / 主机白名单 / multiRegion / OpenAPI 绿；不自动打 tag |

## 签字 / Sign-off

1. 过一遍 scope §2 In（DX79–DX84）/ §3 Out（无公网市场、无计费、非 Active-Active）。  
2. 填写 [`../evidence/v4.3-signatures-template.md`](../evidence/v4.3-signatures-template.md)。  
3. 产品确认范围冻结。

## Tag（人工）

```bash
git tag -a v4.3.0 -m "ASH v4.3.0 private skill catalog and multi-region disabled probe (DX79–DX84 frozen)"
git push origin v4.3.0
```

本清单与 `make v4.3-signoff` **不会**自动执行 `git tag`。
