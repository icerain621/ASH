# ASH v6.5 发布签字清单 / Sign-off checklist（Sprint VX52）

> 目标：冻结 [`v6.5-release-scope.md`](../plan/v6.5-release-scope.md)，跑通 `make v6.5-signoff`，**人工确认后再打 `v6.5.0` tag**。  
> Freeze the v6.5 scope, pass `make v6.5-signoff`, then tag **`v6.5.0` by hand**.  
> 程序：[`../plan/v6.x-program.md`](../plan/v6.x-program.md) · 签字模板：[`../evidence/v6.5-signatures-template.md`](../evidence/v6.5-signatures-template.md)

## 前置 / Preconditions

| # | 项 | 命令 / 文件 | 期望 |
|---|-----|-------------|------|
| 1 | 范围文档 | `doc/plan/v6.5-release-scope.md` | 含 §1–§6；状态「已冻结」 |
| 2 | 结构门禁 | `make scope-freeze-gate` | 含 v6.5 |
| 3 | v6.5 门禁 | `make v6.5-signoff` | Webhook Ingress 绿；不自动打 tag |

## 自动门禁（`make v6.5-signoff`）

- [x] `bash scripts/scope-freeze-gate.sh`（含 v6.5）
- [x] `make openapi-check`
- [x] `go test` ingress + `TestGitHubWebhook`

## 功能收口（VX51 已落地）

- [x] `ASH_INGRESS=webhook-github` 时 HMAC 之后 `Accept`
- [x] 空投递 `INGRESS_REJECTED`，不入库
- [x] 默认 `null` 不改变原 CI 入库路径
- [x] 响应 `ingressAdapter` / `ingressChannel` / `ingressDeliveryId`

## Out（本代不验收）

- Hermes 全 IM · 第二通道 · 改变默认 `ASH_INGRESS` · 新业务表 · 计费 · Cordis · YOLO

## Tag 纪律

本清单与 `make v6.5-signoff` **不会**自动执行 `git tag`。

## 修订

| 日期 | 说明 |
|------|------|
| 2026-09-23 | VX52：v6.5 冻结 + `make v6.5-signoff` |
