# ASH v6.6 发布签字清单 / Sign-off checklist（Sprint VX62）

> 目标：冻结 [`v6.6-release-scope.md`](../plan/v6.6-release-scope.md)，跑通 `make v6.6-signoff`，**人工确认后再打 `v6.6.0` tag**。  
> Freeze the v6.6 scope, pass `make v6.6-signoff`, then tag **`v6.6.0` by hand**.  
> 程序：[`../plan/v6.x-program.md`](../plan/v6.x-program.md) · 签字模板：[`../evidence/v6.6-signatures-template.md`](../evidence/v6.6-signatures-template.md)

## 前置 / Preconditions

| # | 项 | 命令 / 文件 | 期望 |
|---|-----|-------------|------|
| 1 | 范围文档 | `doc/plan/v6.6-release-scope.md` | 含 §1–§6；状态「已冻结」 |
| 2 | 结构门禁 | `make scope-freeze-gate` | 含 v6.6 |
| 3 | v6.6 门禁 | `make v6.6-signoff` | Webhook Notifier 绿；不自动打 tag |

## 自动门禁（`make v6.6-signoff`）

- [x] `bash scripts/scope-freeze-gate.sh`（含 v6.6）
- [x] `make openapi-check`
- [x] `go test` notify + `TestGitHubWebhook`

## 功能收口（VX61 已落地）

- [x] 非重复入库后 `notify.FromEnv().Notify`，事件 `ci.webhook`
- [x] 重复投递不再通知
- [x] 默认 `null` 丢弃且不失败请求
- [x] 响应 `notifier`（`null` 或 `log`）

## Out（本代不验收）

- Hermes 全 IM · 通知失败回滚 CI · 新业务表 · 计费 · Cordis · YOLO

## Tag 纪律

本清单与 `make v6.6-signoff` **不会**自动执行 `git tag`。

## 修订

| 日期 | 说明 |
|------|------|
| 2026-09-23 | VX62：v6.6 冻结 + `make v6.6-signoff` |
