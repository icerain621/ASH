# v4.2 发布签字清单 / Sign-off checklist（Sprint DX78）

> 目标：冻结 [`v4.2-release-scope.md`](../plan/v4.2-release-scope.md)，跑通 `make v4.2-signoff`，**人工确认后再打 `v4.2.0` tag**。  
> Freeze the v4.2 scope, pass `make v4.2-signoff`, then tag **`v4.2.0` by hand**.

## 前置 / Preconditions

| # | 项 | 命令 / 文件 | 期望 |
|---|-----|-------------|------|
| 1 | 范围文档 | `doc/plan/v4.2-release-scope.md` | 含 §1–§6；状态「已冻结」 |
| 2 | 结构门禁 | `make scope-freeze-gate` | 含 v4.2 |
| 3 | v4.2 门禁 | `make v4.2-signoff` | 插件监听 / Register 签名 / Indexer / ABI 姿态绿；不自动打 tag |

## 签字 / Sign-off

1. 过一遍 scope §2 In（DX73–DX78）/ §3 Out（不拆 Session、无独立 Indexer 进程）。  
2. 填写 [`../evidence/v4.2-signatures-template.md`](../evidence/v4.2-signatures-template.md)。  
3. 产品确认范围冻结。

## Tag（人工）

```bash
git tag -a v4.2.0 -m "ASH v4.2.0 Stage-1 plugin grpc production path and in-process RAG indexer contract (DX73–DX78 frozen)"
git push origin v4.2.0
```

本清单与 `make v4.2-signoff` **不会**自动执行 `git tag`。
