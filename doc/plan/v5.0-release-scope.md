# ASH v5.0 发布范围（已冻结 · 双核管控收口）

> 状态：**已冻结**（2026-09-20；前置 **v4.x** DX61–DX84 已分冻收口；功能水位 **GV01–GV06**）  
> Status: **frozen** (2026-09-20). Tag `v5.0.0` is manual.  
> 归属：[`plan/`](README.md)  
> 版本锚点：前置 v4.0–v4.3 功能水位已齐；本代冻结 **已落地的** 薄交互 + 可观测 + 管控/厚评审  
> 程序：[`v5-governance-program.md`](v5-governance-program.md) · [`v4.x-program.md`](v4.x-program.md)（已收口）  
> 关联：[`TODO.md`](TODO.md) · [`sprint-gv06-v50-signoff.md`](sprint-gv06-v50-signoff.md) · [`../checklists/v5.0-signoff.md`](../checklists/v5.0-signoff.md) · [`../evidence/v5.0-signatures-template.md`](../evidence/v5.0-signatures-template.md) · [`../../docs/superpowers/plans/2026-09-13-v5-governance-implementation.md`](../../docs/superpowers/plans/2026-09-13-v5-governance-implementation.md)

## 1. 发布目标

把业已落地的 **薄交互入口 + Session/Thread 可观测 + 双核管控登记 + 厚评审评分** 收成可签字、可打 **`v5.0.0`** 的水位。不引入 Cordis / dsh web，不自动升格，不重开 Agent 打包迁移。  
Freeze the already-shipped thin interaction, Session/Thread observability, dual-core registry/policy, and thick-review scoring into a signable **`v5.0.0`** watermark. No Cordis/dsh web, no auto-promote, no Agent pack migration reboot.

做法 / Approach：A。冻结现有交付与 `make v5-signoff`；不另开厚包实现轨。  
Approach A: freeze the current delivery and `make v5-signoff`; do not open a new heavy implementation track.

## 2. 包含范围（P0 · In）

| 域 | 交付物（水位） |
|----|----------------|
| 薄交互 | visibility · 意图 API · ConversationNode · Quest 去厚（**GV01–03**） |
| 可观测 | interaction threads · Fold/MemoryLink · seal/replay/compare 接口水位（**GV04+**） |
| 管控 + 厚评 | Registry · SpacePolicy · Rubric · Workbench · multi_sign · score_appeal（**P2**） |
| 控制台 IA | 三主题（Agent / 记忆 / 评审）+ Agent Chat DSH 对标主路径 |
| 门禁 | `make v5-signoff` + [`v5.0-signoff.md`](../checklists/v5.0-signoff.md)（**GV06**） |
| 冻结 + 签字 | 本文件冻结；scope-freeze 接入；tag 人工（**GV06**） |

## 3. 不包含范围（本版本 Out）

- Cordis / 嵌入完整 `dsh web` / 一比一 slash 目录  
- 自动升格（promote / memory approve 仍须人审）  
- Agent 一键打包迁移（暂缓）  
- 公网 Marketplace 计费、Active-Active（v4.x Out 延续）  
- 新开厚实现 Sprint（本代以冻结已交付为主）  

No Cordis, no auto-promote, and no new heavy feature track in this freeze.

## 4. 变更冻结规则（GV06 起生效）

- 冻结后仅接受 P0 缺陷、文档/门禁、Out 项环境证据  
- 新表须 SQL rev + RLS + Doctor，并在本文件标明 E5 例外  
- HTTP 变更须 `make openapi-check`  
- 打 **`v5.0.0` tag** 前须签字表 + `make v5-signoff` 绿  

## 5. 验收门禁（GV06）

```bash
make openapi-check
make v5-signoff
```

`make v5-signoff` 含范围冻结校验、OpenAPI、管控/评分/Doctor TR3、控制台 vitest 与 `web-build`。不自动打 tag。  
`make v5-signoff` covers scope freeze, OpenAPI, governance packages, Doctor TR3, console vitest, and web-build. It does not tag.

## 6. 评审签字（GV06 冻结后填）

| 角色 | 姓名 | 日期 | 结论 |
|------|------|------|------|
| 产品 | （待填） | | 范围确认 / 冻结 |
| 技术 | （待填） | | 门禁达标 |
| 测试 | （待填） | | 回归通过 |
| 发布 | （待填） | | 可打 tag（待真人确认） |

### Tag 步骤（人工 · 不自动打标）

```bash
git tag -a v5.0.0 -m "ASH v5.0.0 dual-core governance thin interaction and thick review watermark (GV01–GV06 frozen)"
git push origin v5.0.0
```
