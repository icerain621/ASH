# ASH v6.5 发布范围（已冻结 · Webhook 走 Ingress 缝）

> 状态：**已冻结**（2026-09-23；前置 **v6.4** VX46 已冻结；功能水位 **VX51–VX52**）  
> Status: **frozen** (2026-09-23). Tag `v6.5.0` is manual.  
> 归属：[`plan/`](README.md)  
> 程序：[`v6.x-program.md`](v6.x-program.md) · F4：**无新表** · F5：无公网计费 · F6：不自动打 tag  
> 关联：[`sprint-vx51-webhook-ingress.md`](sprint-vx51-webhook-ingress.md) · [`sprint-vx52-v65-signoff.md`](sprint-vx52-v65-signoff.md) · [`../checklists/v6.5-signoff.md`](../checklists/v6.5-signoff.md) · [`../evidence/v6.5-signatures-template.md`](../evidence/v6.5-signatures-template.md)

## 1. 发布目标

把已有 GitHub Webhook 接到 v6.4 Ingress 缝：`ASH_INGRESS=webhook-github` 时先 `Accept`，失败则拒绝入库；默认 `null` 保持原 HMAC + CI 入库路径。不新增 IM 通道、不新表。  
Route the existing GitHub webhook through the v6.4 ingress seam when `ASH_INGRESS=webhook-github`. Default `null` keeps the current HMAC and CI ingest path. No new IM channel and no new table.

达到可打 **`v6.5.0`** 标签的功能水位。  
Reach the watermark for a manual **`v6.5.0`** tag.

## 2. 包含范围（P0 · In）

| 域 | 交付物（Sprint） |
|----|------------------|
| 活路径 | HMAC 通过后，非 null 适配器 `Accept`；拒绝码 `INGRESS_REJECTED`（**VX51**） |
| 响应标记 | `ingressAdapter` / `ingressChannel` / `ingressDeliveryId` |
| 冻结 + 签字 | 本文件冻结；`make v6.5-signoff`（**VX52**） |

## 3. 不包含范围（本版本 Out）

- Hermes 全 IM、第二通道  
- 改变默认 `ASH_INGRESS`（仍为 null）  
- 新业务表、公网计费、Cordis / YOLO  

No second channel, and the default adapter stays null.

## 4. 变更冻结规则（VX52 起生效）

- 冻结后仅接受 P0 缺陷、文档/门禁  
- HTTP 变更须 `make openapi-check`  
- 打 **`v6.5.0` tag** 前须签字 + `make v6.5-signoff` 绿  

## 5. 验收门禁（VX52）

```bash
make openapi-check
make v6.5-signoff
```

不自动打 tag。Tag is not created by the gate.

## 6. 评审签字（冻结后填）

| 角色 | 姓名 | 日期 | 结论 |
|------|------|------|------|
| 产品 | （待填） | | 范围确认 / 冻结 |
| 技术 | （待填） | | 门禁达标 |
| 测试 | （待填） | | 回归通过 |
| 发布 | （待填） | | 可打 tag（待真人确认） |

### Tag 步骤（人工 · 不自动打标）

```bash
git tag -a v6.5.0 -m "ASH v6.5.0 GitHub webhook through ingress seam (VX51–VX52 frozen)"
git push origin v6.5.0
```
