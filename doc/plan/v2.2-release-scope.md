# ASH v2.2 发布范围（草案）

> 状态：**草案**（2026-08-31；未冻结）  
> 前置：v2.1 已冻结（DW–DX5）  
> 关联：[`v2.1-release-scope.md`](v2.1-release-scope.md)

## 1. 发布目标

在 v2.1 ACP 桥接之上补齐 **持续职责（Waker）** 等运维委派能力。

## 2. 包含范围（拟定）

| Sprint | 交付 |
|--------|------|
| **DX6** | Waker：`/waker/queue|sweep` + 可选后台 `ASH_WAKER_INTERVAL`（report/flag） |
| **DX7** | Waker cancel 闸门：`ASH_WAKER_ALLOW_CANCEL` + confirm 短语 + 审计 |

## 3. 不包含范围

- Landlock / 自动 cancel 生产策略（DX6 仅 report/flag）
- 云 RDS / 真实 live 强制

## 4–6

冻结时补齐变更规则、门禁与签字表（对齐 v2.1 结构）。
