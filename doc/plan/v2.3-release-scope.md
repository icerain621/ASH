# ASH v2.3 发布范围（冻结）

> 状态：**已冻结**（2026-09-01，产品占位确认；真人签字见 [`../checklists/v2.3-signoff.md`](../checklists/v2.3-signoff.md)）  
> 归属：[`plan/`](README.md)  
> 版本锚点：Doctor ALL **55/55** · M4 **8/8** · SQL rev **28** · RLS **48** · Sprint **DX9 / DX10**  
> 前置：v2.2 已冻结（DX6–DX8；tag `v2.2.0` 仍待人工）  
> 关联：[`v2.2-release-scope.md`](v2.2-release-scope.md) · [`sprint-dx9-rag-hybrid.md`](sprint-dx9-rag-hybrid.md) · [`sprint-dx10-rag-hybrid-wiring.md`](sprint-dx10-rag-hybrid-wiring.md)

## 1. 发布目标

在 v2.2 Waker 运维底座上交付 **RAG Hybrid 符号索引（路径 + 符号 + 文本 RRF）** 及其执行/控制台接线，达到可打 **`v2.3.0`** 标签的功能与文档水位（向量库主路径与 ctags 仍为 Out）。

## 2. 包含范围（P0 · 本版本 In）

| 域 | 交付物（Sprint） |
|----|------------------|
| Hybrid Schema | 表 `rag_path_entries` / `rag_symbols`（SQL **28** / RLS **48**）（**DX9**） |
| RebuildSymbols | `POST /rag/symbols/rebuild`；扫盘 upsert + stale 清理；与 chunk Index 解耦（**DX9**） |
| Hybrid Query | path/symbol/text RRF（k=60）；空表回退 FTS/chunk；Profile hybrid 字段（**DX9**） |
| Hybrid 接线 | Run prepare rebuild 事件；`ash rag rebuild`；Knowledge/Scale Hybrid 面板（**DX10**） |
| Doctor / 签字 | `v2.3-release-scope` 冻结；`make v2.3-signoff`（含 `rag-hybrid-smoke`）（**DX11**） |

## 3. 不包含范围（本版本 Out）

- 向量库主路径（Chroma/Qdrant 等）
- ctags / LSP 外部符号解析依赖
- Skill Marketplace、知识图谱
- v2.2 已冻结项的 scope 变更（Waker cancel 策略等）
- 云 RDS 生产切流签字（H-01～H-03）

## 4. 变更冻结规则

- 冻结后仅接受 **P0 缺陷**、**文档/门禁**、以及 Out 项的环境验收证据
- 新表 DDL 必须同步 SQL rev + RLS + Doctor（若触及）
- 破坏性 API 须 OpenAPI + `make openapi-check`
- 打 **`v2.3.0` tag** 前须完成本文件签字表 + `make v2.3-signoff` 绿

## 5. 验收门禁（自动化）

```bash
make scope-freeze-gate    # MVP + v2 + v2.1 + v2.2 + v2.3 scope 结构
make v2.3-signoff         # v2.3 结构门禁 + Doctor ALL + rag-hybrid-smoke
make openapi-check
go test ./internal/doctor/... -run 'Test(M4|ALL)Suite' -count=1
make rag-hybrid-smoke
```

## 6. 评审签字

| 角色 | 姓名 | 日期 | 结论 |
|------|------|------|------|
| 产品 | 产品负责人（占位） | 2026-09-01 | 范围确认 / 冻结 |
| 技术 | 技术负责人（占位） | 2026-09-01 | 门禁达标（代码水位） |
| 测试 | 测试负责人（占位） | 2026-09-01 | 回归通过（Doctor ALL 55） |
| 发布 | 发布负责人（占位） | 2026-09-01 | 可打 tag（待真人确认） |

### Tag 步骤（人工确认后执行，本 Sprint **不自动打标**）

```bash
git tag -a v2.3.0 -m "ASH v2.3.0 RAG Hybrid (DX9–DX11 frozen)"
git push origin v2.3.0
```
