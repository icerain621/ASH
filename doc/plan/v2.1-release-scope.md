# ASH v2.1 发布范围（草案）

> 状态：**草案**（2026-08-30；未冻结）  
> 归属：[`plan/`](README.md)  
> 前置：v2.0 功能水位 DH–DV（tag `v2.0.0` 仍待人工）  
> 关联：[`v2-release-scope.md`](v2-release-scope.md) · [`sprint-dw-acp-provider.md`](sprint-dw-acp-provider.md)

## 1. 目标

在 v2.0 冻结范围之上，补齐 **ACP 控制面桥接** 与后续企业委派能力，不回滚 v2 Out 项（云 RDS / 强制 ExecGo live 仍为环境门禁）。

## 2. In（拟定）

| Sprint | 交付 |
|--------|------|
| **DW** | ACP provider 骨架：`ACPExecutor` + Probe + `/providers/agent.acp` + fallback |
| DX3+（待排） | ACP 协议完备 / Session 互通 / Landlock 可选 |

## 3. Out（仍属环境或更后）

- 云 RDS 切流签字（H-01～H-03）
- ExecGo/GitHub **真实 live** 强制（脚本已有，需密钥）
- 向量库主路径、Skill Marketplace、外部 IdP

## 4. 变更规则（草案）

- 冻结前可合入 DW 类无新表增量
- 新表仍须 SQL rev + RLS + Doctor
- 打 `v2.1.0` 前另开签字清单（本文件升级为冻结态时）
